<h1>Effort Blocks</h1>

<%= link_to 'New Block', new_discipline_block_path %>

<% @blocks.each do |block| %>

  <p>
    <%= block.discipline %><br>
    <%= block.start %><br>
    <%= block.finish %><br>
    <%= link_to 'Destroy', block_path(block), method: :delete %>
  </p>

<% end %>
