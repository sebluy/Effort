<div class="page-header">
  <h1>
    Disciplines
    <%= link_to 'New Discipline', new_discipline_path,
            class: 'btn btn-large btn-primary pull-right' %>
  </h1>
</div>

<table class="table">
  <%= render 'table_header' %>
  <tbody>
    <%= render @disciplines %>
  </tbody>
  <%= render 'table_footer' %>
</table>

<%= hidden_field_tag 'plot_data', @summary.to_json %>
<%= render 'plots' %>
